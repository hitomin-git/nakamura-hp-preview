# Issue #7 セクション順序確認

- 対象: `index.html`。規模S。
- ベース: PR #6（`cf0dd42`）。
- `#reviews` 全体を `#concerns` の直前へ移動。PCとスマホの見出し文言は維持。

## 確認結果

- `git diff --check`: PASS。
- 移動前後のセクション文字列比較: PASS（Windows改行を正規化して比較）。口コミセクションの内容と、それ以外の内容が変わっていないこと、concerns直前であることを確認。
- 初回の比較はCRLF/LF差で失敗。改行を正規化した再確認でPASS、内容変更なし。
- 親担当が1151px PC・390pxスマホのDOMとスクリーンショットで確認: 順序正常、見出し維持、余白が既存セクションとして自然。PASS。
- セキュリティ差分確認: セクションの移動のみ。通信・権限・機密情報追加なし。

## 反映

- ローカル: `http://127.0.0.1:8765/index.html#reviews`。
- 公開テストURLはmain配信のため、PRマージ待ち。
